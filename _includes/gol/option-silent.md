### `-s`, `--silent` {#option-silent}

No output at all is written to `stderr`, not even error messages.
(Whether a command succeeded or failed can only be ascertained via the status code
returned by the process).